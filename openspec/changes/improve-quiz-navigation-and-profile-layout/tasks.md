## 1. 答题浏览状态

- [x] 1.1 在答题页增加独立于持久化 `currentIndex` 的页面级 `viewIndex`，并验证首次进入、未完成练习恢复和题目列表变化时索引始终落在有效范围内
- [x] 1.2 根据 `viewIndex` 和题目 ID 从现有答题记录恢复历史题的原选择、正误、正确答案及解析，并验证历史答案为只读且不会重复提交或改分
- [x] 1.3 实现“上一题”和回看状态下的“下一题”逻辑，并验证第一题不能后退、逐题回看及逐题返回最远答题位置均符合 `quiz-question-navigation` 规格
- [x] 1.4 隔离浏览位置与服务端断点保存，验证回看期间的自动保存、退出再进入和恢复练习均保留原最远 `current_index` 与完整答案记录
- [x] 1.5 适配增量生成轮询，验证回看时追加的新题不会强制跳转，前沿暂无新题时仍能进入现有继续生成等待状态并在新题到达后继续作答

## 2. 按钮与返回控件统一

- [x] 2.1 盘点前端二级页面现有自定义返回控件和导航回调，形成明确接入清单，并通过代码检索确认没有漏掉同类返回按钮
- [x] 2.2 实现轻量共享返回控件或公共样式契约，统一顶部左侧安全区位置、图标、形状、尺寸、至少 44px 等效触控区和按下反馈，并用窄屏与不同状态栏高度检查无遮挡
- [x] 2.3 将接入清单内的二级页面替换为统一返回控件，同时保留各页面原有导航回调，并逐页验证返回目标和退出前保存行为没有改变
- [x] 2.4 将答题页底部改为左侧次要“上一题”与右侧主要操作的稳定双按钮布局，验证各题号及“提交答案”“下一题”“查看本轮报告”标签切换时位置、高度、圆角均一致且文字不溢出
- [x] 2.5 检查底部安全区和题目内容留白，验证 320px 等效窄屏、长题干、长解析及带配图场景下按钮不遮挡内容、页面不横向溢出

## 3. 个人页布局

- [x] 3.1 将昵称修改入口移动到用户信息区的昵称旁，并验证普通昵称、最大长度昵称和不同 XP 文本下头像、昵称、修改入口及 XP 不重叠
- [x] 3.2 将输入、保存和取消状态内联到昵称区域，复用现有资料更新接口，并验证保存成功、取消编辑、非法昵称和接口失败时的显示与提示正确
- [x] 3.3 删除退出登录按钮下方的“返回答题”节点及其专用样式，并验证底部“答题”Tab 仍可直接切换到答题首页
- [x] 3.4 回归头像查看与裁剪上传、历史记录、知识库入口、XP 展示和退出登录，确认个人页布局修改未改变任何现有业务行为

## 4. 自动化与构建验证

- [x] 4.1 为浏览索引、历史答案恢复、进度不倒退和增量追加题目补充可用的前端状态/交互测试；若仓库暂无对应测试运行器，则提取纯函数并以类型检查加可复现手工用例验证关键状态转换
- [x] 4.2 在 `frontend` 运行 `npm run typecheck`，修复全部 TypeScript 错误并保存成功输出作为验收记录
- [x] 4.3 在 `frontend` 运行 `npm run build:weapp`，确认微信小程序产物完整生成且无新增编译错误或资源缺失
- [x] 4.4 运行现有后端 pytest 回归套件，确认本次纯前端改动未伴随接口或数据模型变更且现有核心接口测试保持通过

## 5. 微信开发者工具全流程验收

- [ ] 5.1 在微信开发者工具中完成“生成第一题 -> 连续作答 -> 上一题回看 -> 下一题返回 -> 完成报告”流程，并核对原选择、正误和解析展示正确
- [ ] 5.2 在题目后台继续生成时进行上一题回看、退出答题页和未完成练习恢复，确认无空白页、无强制跳题、无 409 进度冲突且恢复点不倒退
- [ ] 5.3 逐页肉眼检查统一返回控件与答题底部按钮在至少一个常规 iPhone、一个窄屏设备和一个带底部安全区设备上的位置、形状、尺寸、间距和点击反馈
- [ ] 5.4 验证个人页昵称编辑新位置、头像功能、退出登录和底部 Tab 切换，确认“返回答题”冗余按钮已移除且控制台无新增报错
