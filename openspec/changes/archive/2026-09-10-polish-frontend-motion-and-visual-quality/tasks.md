## 1. 视觉基线与全局规范

- [x] 1.1 记录首页、题目生成、答题、报告生成、报告、个人中心、知识库、历史记录和历史详情的现有微信端截图与关键尺寸，验证每个页面都有可对比的改造前基线
- [x] 1.2 在全局样式中建立颜色、文字、间距、圆角、阴影和动效 token，验证页面可以引用 token 且不改变现有业务行为
- [x] 1.3 增加快速反馈、状态切换、内容进入和阶段切换的统一缓动及时长规则，验证规则覆盖普通与弱动效状态
- [x] 1.4 盘点并标记各页面重复的主按钮、次按钮、返回按钮、卡片和状态样式，验证迁移清单覆盖全部现有前端页面

## 2. 公共交互控件

- [x] 2.1 统一主按钮、次按钮、危险按钮和禁用/加载状态，验证不同页面的相同按钮尺寸、圆角、字体和按压反馈一致
- [x] 2.2 统一图标按钮、返回按钮、菜单项、列表箭头和可点击卡片反馈，验证按压时布局不位移且触控区域不小于现有尺寸
- [x] 2.3 统一分段控件、开关周边状态和题目选项的选中/禁用反馈，验证快速连续点击不会造成状态闪烁或重复业务调用
- [x] 2.4 清理已完成迁移的重复样式定义，运行微信小程序构建并验证无选择器覆盖或样式缺失

## 3. 首页与题目生成体验

- [x] 3.1 优化首页标题、输入区、设置面板、未完成练习和主操作区的视觉层级与间距，通过 375px 等效宽度截图验证首屏无拥挤、空洞或文字溢出
- [x] 3.2 为首页核心区域增加一次性进入编排，并为设置展开、练习卡片和生成按钮增加统一过渡，验证 TabBar 返回时不会反复播放冗长动画
- [x] 3.3 实现核心流程的“答题节奏线”公共视觉，并接入题目生成页面的真实任务阶段，验证阶段只随已有任务状态前进且不会用计时伪造完成
- [x] 3.4 优化生成成功、第一题就绪、失败和重试状态，验证第一题可用时立即进入答题页且失败时循环动画停止

## 4. 答题页面动效与状态安全

- [x] 4.1 优化答题页题号、来源状态、题干、选项、解析、配图和底部操作区的视觉层级，验证长题干、长选项和配图不会遮挡操作按钮
- [x] 4.2 为选项按下、选中、提交、正确/错误结果和解析展开增加顺序明确的动效，验证动画中断后仍直接显示最终正确状态
- [x] 4.3 为上一题和下一题实现方向一致的题目切换过渡，验证返回上一题时已选答案、反馈和进度保持不变
- [x] 4.4 优化等待下一道增量题目的非阻塞状态，验证下一题到达后继续作答且不会出现空白页、错误跳转或当前题目丢失
- [x] 4.5 验证快速提交、快速前进/后退、页面隐藏和恢复期间不会重复保存、重复导航或回退到旧题目状态

## 5. 报告生成与报告展示

- [x] 5.1 将答题节奏线接入报告生成页面，并优化生成中、成功、失败和重试状态，验证报告可用后不会被非必要动画延迟展示
- [x] 5.2 为报告首屏分数、正确/待巩固统计和知识点进度增加克制的进入动画，验证长题目结果列表即时可滚动且不逐项延迟
- [x] 5.3 统一报告题目结果、答案、解析、配图和操作按钮的间距及状态颜色，验证所有题目答题情况完整展示且图片后续内容可见
- [x] 5.4 验证报告数据缺失、图片失败和报告生成失败场景仍提供清晰静态内容与可执行操作

## 6. 个人中心、知识库与历史记录

- [x] 6.1 优化个人中心头像、昵称编辑、XP、统计和菜单层级，并增加必要的状态过渡，验证登录、退出登录和头像编辑后页面信息立即正确更新
- [x] 6.2 优化知识库上传、解析、索引、成功、失败、重命名和删除状态，验证进度由真实数据驱动且旧响应不会让状态倒退
- [x] 6.3 优化知识库文档卡片和“开始答题”操作反馈，验证进入仅使用个人知识库的答题流程保持原有行为
- [x] 6.4 优化历史记录和历史详情列表、分数、题目解析及配图展示，验证返回导航、长列表滚动和查看完整报告正常

## 7. 弱动效、性能与兼容性

- [x] 7.1 增加弱动效规则，使支持减少动态效果的环境关闭位移和循环动画，验证关闭动画后所有页面直接呈现最终状态
- [x] 7.2 检查所有动画仅使用必要的 transform、opacity、颜色或阴影变化，验证不存在依赖动画完成事件的业务提交、保存或导航
- [x] 7.3 检查页面隐藏、卸载和快速切换时的动画、定时器与轮询清理，验证重复进入页面不会累积动画或后台任务
- [x] 7.4 检查微信小程序包体和构建产物，验证未引入大型动画/UI 依赖且现有原生 TabBar 配置保持不变

## 8. 自动化与构建验证

- [x] 8.1 为可抽离的题目切换方向、阶段单调更新和弱动效状态增加前端测试，运行相关测试并验证全部通过
- [x] 8.2 运行 `npm run typecheck`，验证 TypeScript 无错误
- [x] 8.3 运行 `npm run test:quiz-navigation`，验证上一题、下一题和边界导航逻辑无回归
- [x] 8.4 运行 `npm run build:weapp`，验证 Taro 4.2.1 微信小程序生产构建成功
- [x] 8.5 运行后端 pytest 回归测试，验证纯前端改造未伴随核心接口或业务行为回归

## 9. 微信开发者工具与肉眼验收

- [x] 9.1 在微信开发者工具中验证游客与登录用户的“答题/我的”TabBar 切换，确认无空白、重复页面、失效点击或重复开场动画
- [x] 9.2 完整验证“输入主题 -> 生成第一题 -> 连续答题 -> 上一题 -> 等待后续题 -> 生成报告 -> 查看全部题目结果”流程，并记录关键阶段截图
- [x] 9.3 验证生成失败、报告失败、图片失败、上传失败和接口慢响应场景，确认循环动画停止条件、错误提示和重试操作正确
- [x] 9.4 验证头像更换、昵称修改、退出登录、知识库上传/重命名/删除/开始答题和历史详情流程，确认业务功能与视觉状态一致
- [x] 9.5 在至少一个窄屏和一个较高分辨率设备模拟器中肉眼检查文字、按钮、卡片、配图、底部安全区和滚动，确认无重叠、截断或不合理间距
- [x] 9.6 对比改造前后截图并完成最终视觉审查，确认动效强度为中等、页面保持专业可信且不存在无意义装饰动画
