## 1. 路由基础模型与配置

- [x] 1.1 定义统一的路由计划、证据项、证据上下文和工具调用元数据模型，并用单元测试验证 JSON 序列化、默认值和向后兼容字段
- [x] 1.2 增加路由开关、单次请求工具预算、超时、上下文长度、缓存 TTL 和证据评分校准参数，并验证缺省配置下现有服务可以启动
- [x] 1.3 实现 URL、最新版本要求、个人资料要求和无知识库状态的确定性门控，并用参数化测试覆盖中文、英文和混合输入

## 2. 统一检索工具与 Router Agent

- [x] 2.1 将个人 Chroma 相似度检索封装为带用户隔离约束的 Agent Tool，并测试 ready 文档、空知识库和检索异常场景
- [x] 2.2 重构 Tavily Search/Extract 工具描述和结构化路由输出，使 Agent 能选择搜索、页面提取或组合调用，并测试非法计划会被预算校正
- [x] 2.3 实现主题查询改写策略，保留原始技术术语并在需要时生成中文和英文搜索查询，验证 Harness Engineering 等新术语的查询结果
- [x] 2.4 实现简单、复杂、版本敏感主题的 Tavily 参数策略，验证 search depth、结果数量和 extract depth 在预算范围内变化

## 3. 证据评估与混合编排

- [x] 3.1 实现本地证据的相关性、覆盖度、可回答性和冲突评估，并用标注样本测试高、中、低置信度分类
- [x] 3.2 实现高置信度本地直用、中置信度联网补充、低置信度联网替代和冲突资料追加权威来源的编排逻辑，并用 mock 工具验证每条路径
- [x] 3.3 实现 EvidenceContext 的去重、重排、来源标记、URL/抓取时间保留和最大上下文裁剪，并测试个人资料与网络资料混合输入
- [x] 3.4 将题目生成器改为只接收统一 EvidenceContext，移除同一请求中重复研究调用的路径，并用调用计数测试确认 Tavily 不会重复触发

## 4. 成本、可靠性与降级

- [x] 4.1 增加公共 Tavily 搜索/提取结果缓存并确保缓存键不包含或泄露其他用户的个人知识库内容，使用隔离测试验证缓存命中和过期
- [x] 4.2 实现 Router、Chroma、Tavily 和 Evidence Grader 的超时、异常分类、结构化日志和调用预算，使用故障注入测试验证超时可恢复
- [x] 4.3 实现 Tavily 失败时依次使用高质量本地证据、已有证据和基础模型的降级顺序，并验证生成接口仍返回兼容的题目或失败结构
- [x] 4.4 为低置信、联网或资料冲突请求增加一次受控生成校验/重试，验证重试不会再次发起相同的网络检索

## 5. 题目生成兼容性

- [x] 5.1 将路由、来源、调用次数、耗时和降级原因写入可选 evidence metadata，并验证旧客户端忽略新增字段时题目结构不变
- [x] 5.2 更新题目生成 Prompt，明确个人资料和网络资料的来源边界、版本冲突处理和禁止编造引用规则，并用固定样例测试生成结果
- [x] 5.3 验证非技术主题仍被领域判断拦截，未知技术术语仍可进入检索流程，且现有答题、报告和断点续答接口行为不变

## 6. 测试与验收

- [x] 6.1 补充后端 pytest 单元测试，覆盖路由门控、Agent 计划、证据评分、混合检索、缓存、预算、超时和降级
- [x] 6.2 增加 Chroma、Tavily Search、Tavily Extract 和 DeepSeek 的 mock 集成测试，验证普通主题、URL、最新版本、空知识库和 Harness Engineering 场景
- [x] 6.3 运行完整后端 pytest 并确认所有既有测试和新增测试通过，同时输出路由命中率、网络触发率和失败率的测试统计
- [x] 6.4 运行前端 typecheck 和微信小程序构建，确认生成中、生成失败和生成成功页面无需修改即可兼容新增证据元数据
- [x] 6.5 在微信开发者工具中验证普通主题、个人资料命中、低置信联网补充、URL 提取、Tavily 失败降级和现有答题报告核心流程
