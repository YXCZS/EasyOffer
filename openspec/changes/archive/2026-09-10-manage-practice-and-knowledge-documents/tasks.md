## 1. 后端练习记录管理

- [x] 1.1 补充练习放弃/删除接口的状态与幂等测试，覆盖 `in_progress`、`ready_for_report`、`completed`、重复请求和跨用户请求，并确保现有 `abandoned` 逻辑满足规格
- [x] 1.2 将练习删除接口错误映射为稳定的 API 响应，验证数据库连接异常、已完成状态和不存在记录不会泄露题目内容

## 2. 后端知识库名称管理

- [x] 2.1 新增文档名称请求模型与 `PATCH /api/v1/knowledge/documents/{document_id}` 路由，验证登录、归属、空白名称和长度边界
- [x] 2.2 在知识库仓储中实现带用户条件的名称更新，验证只更新 `original_name`，不改变文档 ID、文件路径、状态、分片数量和向量处理记录
- [x] 2.3 为名称更新补充 pytest API/仓储测试，覆盖成功修改、跨用户拒绝、文档不存在、非法名称和重复提交

## 3. 前端交互与文案

- [x] 3.1 在首页未完成练习卡片增加删除操作、确认弹窗、请求中禁用和成功后刷新，验证取消删除不发请求、重复删除不会残留卡片
- [x] 3.2 在知识库卡片实现名称内联编辑、保存/取消和错误恢复，验证保存后列表立即显示新名称且不影响开始答题参数
- [x] 3.3 将 ready 文档入口及相关可见文案统一改为“开始答题”，验证 processing/failed 文档不显示可执行入口且现有路由仍携带 `document_id`
- [x] 3.4 扩展前端 API 类型与错误处理，运行 `npm run typecheck` 验证类型无误

## 4. 集成验证与交付

- [x] 4.1 运行后端完整 `python -m pytest -q`，验证原有答题、进度恢复、知识库上传和删除流程不回归
- [x] 4.2 运行 `npm run build:weapp`，验证微信小程序产物可以编译
- [x] 4.3 已尝试在微信开发者工具验证未完成练习删除、文档改名后开始答题和“开始答题”文案；CLI 可连接 IDE，但受 AppID 详细信息权限接口失败影响，无法进入页面自动化，限制已记录
