## 1. 研究规划模型

- [x] 1.1 定义查询规划的结构化输入输出模型，包含规范主题、意图、关键术语和最多 3 个查询；通过非法 JSON、空查询、重复查询和偏离主题测试验证校验与原始查询回退
- [x] 1.2 实现 DeepSeek 查询规划调用、一次重试和阶段超时；通过模拟成功、超时、限流和结构化输出失败测试确认 `query_planner_fallback` 原因可观测且不阻塞任务创建
- [x] 1.3 为查询规划增加 Prompt 约束，确保原始主题、岗位方向和面试语境被保留；通过 React Hooks、多知识点和 Harness Engineering 样例断言查询语义覆盖

## 2. Tavily 搜索与 URL 提取

- [x] 2.1 将扩展查询接入 Tavily Search 并行执行，限制查询数、每次结果数、search_depth 和总超时；通过部分成功/部分失败测试确认成功结果仍被合并
- [x] 2.2 保留 Tavily Search/Extract 的独立重试和错误分类；通过 429、5xx、超时、空响应和无效 URL 测试确认 `web_search_failed`/`web_extract_failed` 降级信息
- [x] 2.3 为 URL Extract 增加正文长度限制和章节/段落内容块切分；通过导航、广告、长正文和空正文样例确认候选块边界稳定且 URL 引用保留
- [x] 2.4 更新研究缓存键和研究日志，纳入 query planner/filter 版本及候选/保留数量；通过相同主题不同版本缓存测试确认不会复用旧的未过滤结果

## 3. 搜索证据后处理

- [x] 3.1 实现 URL、标题和正文指纹去重及候选结果上限；通过跨查询重复 URL、相似标题和空内容测试确认只保留最佳候选并保留来源元数据
- [x] 3.2 定义 AI 相关性过滤结构化输出，包含评分、保留标记、理由、知识主张、版本适配和冲突分组；通过有效和非法过滤响应测试确认 Schema 校验
- [x] 3.3 实现搜索结果和 URL 内容块的统一 AI 过滤，按阈值和最多 5 条证据生成 `EvidenceContext`；通过高相关、低相关、全低分和冲突来源测试确认仅保留合格证据
- [x] 3.4 实现过滤一次重试及失败降级；通过过滤超时、非法 JSON、服务错误和无相关结果测试确认未过滤内容不会注入出题 Prompt，并记录对应 fallback reason

## 4. Agentic RAG 与增量出题集成

- [x] 4.1 将关键词研究流程改为“规划 -> 并行搜索 -> 去重 -> AI 过滤 -> 证据上下文”，URL 流程改为“Extract -> 分块 -> AI 过滤”；通过路由测试确认文档专属模式跳过全部公共联网工具
- [x] 4.2 将过滤后证据和研究元数据接入现有增量题目准备与单题 Prompt；通过测试确认题目只看到保留证据、来源可追溯且已有答题记录不被覆盖
- [x] 4.3 保持创建接口异步、首题提前可用、后续题轮询追加和基础模型降级；通过真实/模拟任务测试确认研究阶段失败不会造成空白页或整组失败

## 5. 回归测试与质量验证

- [x] 5.1 增加后端 TDD 覆盖：查询扩展、并行搜索合并、URL 分块、AI 过滤阈值、来源冲突、各类失败降级和缓存版本；运行 `pytest -q` 全部通过
- [x] 5.2 运行 `python -m compileall -q app`，并检查研究日志不包含 API Key、完整敏感输入或网页指令内容
- [ ] 5.3 使用真实 DeepSeek/Tavily 验证口语化多知识点、Harness Engineering、URL 页面、部分搜索失败和过滤失败，记录查询数、候选数、保留数、耗时和最终题目唯一性
- [x] 5.4 运行前端 `npm run typecheck` 与 `npm run build:weapp`，确认现有生成页、答题页、报告页无需新增搜索时效字段且构建无回归
- [x] 5.5 在微信开发者工具验证普通联网主题、URL 主题、首题跳转、后续题追加、研究降级、答题恢复和报告入口；确认无空白页、重复题覆盖或未处理 409
