## 1. 后端逐题生成

- [x] 1.1 将创建接口改为异步任务入口：创建并持久化任务后立即返回任务 ID 和运行中状态，资料准备与模型调用由后台执行；通过 API 延迟测试确认创建请求不等待搜索或整套题生成
- [x] 1.2 移除生成任务对六题蓝图的前置等待，改为后台资料准备完成后直接进入第 1 题单题生成；通过单元测试确认任务不会调用或等待完整蓝图
- [x] 1.3 将生成循环拆分为单题生成、校验、持久化步骤；通过测试确认每次只追加一道题且 `generated_count` 按 1、2、3 顺序递增
- [x] 1.4 更新单题 Prompt，传入当前主题、岗位、难度、证据上下文和全部已生成题目摘要；通过模拟模型测试确认后续题收到之前题目的去重信息
- [x] 1.5 保留题目结构、相关性和重复校验，并实现当前题独立重试与降级；通过非法 JSON、字段缺失、重复题和单题超时测试确认已有题目不受影响
- [x] 1.6 完善生成任务状态和版本控制，确保每次追加题目后立即可被任务查询接口读取；通过 API 测试确认首题快照包含 1 道题且版本递增

## 2. 前端增量答题体验

- [x] 2.1 调整生成页轮询为单飞请求，正常间隔约 6 秒，网络异常或响应慢时退避约 8 秒，并在终态停止；通过计时或模拟请求测试确认无并发轮询
- [x] 2.2 在首次快照发现 `questions.length >= 1` 时跳转答题页，不等待 `completed` 或总题数；通过前端状态测试确认首题出现即可导航
- [x] 2.3 在答题页继续轮询并合并新增题目，当前题未生成时显示等待状态；通过模拟快照序列测试确认题目顺序、当前题号和答案不回退
- [x] 2.4 处理部分失败和重试入口，保留已生成题目与答题记录；通过失败后重试测试确认不会出现空白页或清空题组
- [x] 2.5 保持生成期间进度保存、退出恢复和 409 冲突重试；通过并发保存测试确认本地答案最终与服务端一致

## 3. 集成验证与回归

- [x] 3.1 增加后端 TDD 覆盖：首题提前可用、逐题顺序写入、重复题重试、部分任务恢复和完成态校验；运行 `pytest -q` 全部通过
- [x] 3.2 运行 `python -m compileall -q app`、前端 `npm run typecheck` 和 `npm run build:weapp`，确认无编译错误
- [x] 3.3 使用真实 DeepSeek、Chroma/Tavily 配置创建一个新主题任务，记录每次快照的状态、题目数和耗时，确认首题先出现且后续题在答题期间追加
- [x] 3.4 在微信开发者工具验证生成页、首题跳转、等待下一题、继续答题、返回恢复和报告入口；确认控制台无空白页、重复题覆盖或未处理 409
- [x] 3.5 验证异常网络、模型失败和任务重试场景，确认 6 秒正常轮询、8 秒退避和终态停止行为符合规格
