## 1. 后端题型契约

- [x] 1.1 先为 4/1/1 题型配额、题号目标题型、判断题选项和完整题组校验编写失败测试，并验证新增测试在实现前按预期失败
- [x] 1.2 实现同步与增量共用的题型契约函数，并验证单元测试覆盖单选、多选、判断及非法题型边界
- [x] 1.3 扩展 `Question`/`Quiz` 校验：判断题限定两个“正确/错误”选项、完整题组限定 4/1/1，并验证既有单选历史模型仍可读取

## 2. 后端生成链路

- [x] 2.1 更新同步出题 Prompt，明确 4 道单选、1 道多选、1 道判断及各题答案约束，并通过同步生成服务测试验证配额错误会触发重试
- [x] 2.2 更新增量单题 Prompt，按题号传入确定性目标题型且不增加六题蓝图调用，并通过测试验证每次生成请求获得正确类型
- [x] 2.3 在增量题目写入任务快照前校验目标题型，错误类型进入现有单题重试，并验证错误题型不会递增 `generated_count`
- [x] 2.4 使增量降级题按当前目标题型生成合法答案和选项，并验证模型连续失败后最终题组仍满足 4/1/1
- [x] 2.5 增加同步和异步 API 回归测试，验证返回题组、任务快照、保存进度和报告接口兼容三种题型

## 3. 前端答题交互

- [x] 3.1 提取或复用题目选择与判题纯函数，为多选切换、无序集合判题、单选/判断互斥行为编写前端测试并通过
- [x] 3.2 在答题页为多选题增加“可多选”提示，保持三种题型标签和按钮尺寸与现有 UI 一致，并通过 typecheck 和组件/行为测试
- [x] 3.3 验证多选题未选答案不能提交、提交后只读、上一题回看可恢复全部选项，使用前端自动化测试覆盖这些状态
- [x] 3.4 验证判断题只显示“正确/错误”并保持互斥选择，使用前端自动化测试覆盖切换和提交状态

## 4. 进度、历史与报告

- [x] 4.1 增加进度保存与 409 合并测试，验证多选答案数组在本地、增量任务和正式进度之间不丢失、不降级
- [x] 4.2 增加报告评分测试，验证多选题仅在答案集合完全一致时得分，判断题按单一答案得分
- [x] 4.3 增加报告页和历史详情测试，验证用户答案、标准答案、题型和解析能正确回显三种题型

## 5. 全量验证

- [x] 5.1 运行 `pytest -q`，修复回归并确认全部后端测试通过
- [x] 5.2 运行 `npm run typecheck`、`npm run test:frontend`、`npm run test:quiz-navigation` 和 `npm run build:weapp`，确认前端检查与微信构建通过
- [x] 5.3 使用真实 DeepSeek 连续生成至少 3 组题，记录每组 4/1/1 组成并人工检查多选干扰项、判断题语义和答案有效性
- [x] 5.4 在微信开发者工具完成普通主题和个人知识库主题全流程：生成首题、后台补题、三种作答、返回恢复、报告和历史回看，并逐项记录验收结果
- [x] 5.5 运行 `openspec validate add-multiple-choice-and-true-false --strict`，确认变更规格通过严格校验
