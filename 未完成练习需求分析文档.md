# 《EasyOffer 未完成练习与断点续答》需求分析文档

> 文档版本：v1.0  
> 编写日期：2026-08-23  
> 项目阶段：用户系统扩展 / 需求确认  
> 产品形态：微信小程序  
> 目标用户：中国大陆中文程序员技术面试准备者

## 1. 需求背景

当前 EasyOffer 已经支持“输入技术主题 → AI 生成题目 → 用户答题 → 生成报告”的核心流程。现有答题页左上角返回操作会清空答题会话并重新进入首页，这会导致用户已经完成的答题进度丢失，用户无法在稍后继续练习。

本次需求的核心不是“退出答题”，而是“暂时离开答题并保留进度”：

1. 用户在答题过程中点击左上角返回按钮，系统应保存当前题组、当前题目位置和已提交答案。
2. 用户下次打开小程序或重新登录后，首页应展示未完成练习。
3. 用户点击未完成练习后，应回到上次离开的题目位置继续答题。
4. 只有用户完成整组题目并生成报告后，该练习才算完成，不再出现在未完成列表中。

## 2. 需求目标

### 2.1 产品目标

建立可靠的断点续答能力，降低用户中途退出的损失，让一次练习从“临时页面状态”升级为“可恢复的学习任务”。

### 2.2 用户价值

1. 用户可以随时离开，不必担心已经完成的题目丢失。
2. 用户可以在首页快速找到上次未完成的主题。
3. 用户在不同时间重新打开小程序后，仍能接着上次的进度学习。
4. 用户不会因为误触返回、切换底部 Tab 或小程序被系统回收而重复答题。

### 2.3 本次需求的成功标准

用户完成部分题目后离开答题页，再次进入小程序，能够看到该练习的主题、进度和“继续答题”入口，并从离开时的题目继续完成，最终正常生成报告。

## 3. 用户场景

### 场景一：主动暂离

用户正在练习“Redis 持久化”，已完成前 2 道题，点击答题页左上角返回按钮。系统保存进度并回到答题首页，首页显示“Redis 持久化，已完成 2/6 题”，用户稍后点击“继续答题”恢复第 3 题。

### 场景二：切换到“我的”

用户答题到第 4 题时点击底部“我的”。系统保存当前答题状态，用户查看个人中心后再次点击“答题”或首页的未完成练习，继续第 4 题。

### 场景三：小程序被关闭

用户答题过程中直接关闭小程序或微信开发者工具。重新打开并完成登录后，首页仍能看到该未完成练习。

### 场景四：多次未完成练习

用户先后开始“Java 并发”和“RAG 技术”两组练习，但都没有完成。首页按最近更新时间展示多个未完成练习，用户可以分别继续，不应只能保留最后一组。

### 场景五：完成练习

用户完成最后一道题并成功生成报告。该练习状态变为“已完成”，从首页未完成模块移除，但仍保留在练习历史中，可在历史详情查看报告。

### 场景六：生成报告失败

用户已经答完全部题目，但报告生成失败。该练习不应被误判为完成，应保留“已答完，待生成报告”状态，用户可以重新生成报告。

## 4. 需求范围与优先级

### 4.1 P0：必须实现

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| P0-1 | 保存答题进度 | 保存题组、当前题目索引、已提交答案、最后更新时间和练习状态 |
| P0-2 | 返回按钮保留进度 | 左上角返回不再清空会话，保存后返回答题首页 |
| P0-3 | 首页未完成模块 | 首页展示当前用户未完成的练习列表或最近一条未完成练习 |
| P0-4 | 继续答题 | 点击未完成练习，恢复到上次离开的题目 |
| P0-5 | 完成后移除 | 报告成功生成后，练习从未完成模块消失并进入历史记录 |
| P0-6 | 登录后同步 | 用户每次登录或打开小程序后，能够读取自己的未完成练习 |
| P0-7 | 异常恢复 | 小程序意外关闭、网络中断或页面返回时，已提交的答案不能丢失 |

### 4.2 P1：建议实现

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| P1-1 | 未完成进度展示 | 显示已完成题数、总题数、当前题目、最近学习时间 |
| P1-2 | 多组未完成练习 | 支持用户同时保留多组进行中的题目 |
| P1-3 | 删除未完成练习 | 用户可以主动放弃并删除某组未完成练习，删除前需要二次确认 |
| P1-4 | 继续生成报告 | 全部题目已答完但报告失败时，提供“重新生成报告”入口 |
| P1-5 | 本地与服务端一致性 | 登录前产生的本地进度，在登录成功后尝试合并到用户账户 |

### 4.3 本次不做

1. 不做跨设备实时同步，登录后重新进入时同步即可。
2. 不做自动提醒、推送通知和学习计划。
3. 不做复杂的练习排序算法，首版按最近更新时间倒序。
4. 不做练习题内容的局部编辑或重新出题。
5. 不做多人共享练习和排行榜。

## 5. 核心业务规则

### 5.1 练习状态

每组练习必须有明确状态：

| 状态 | 含义 | 是否展示在未完成模块 |
| --- | --- | --- |
| `in_progress` | 已生成题目，尚未答完 | 是 |
| `ready_for_report` | 所有题目已答完，报告尚未成功生成 | 是 |
| `completed` | 报告成功生成 | 否 |
| `abandoned` | 用户主动放弃并删除/关闭该练习 | 否 |

状态只能按以下方向变化：

```text
生成题组 -> in_progress -> ready_for_report -> completed
                 └------> abandoned
```

生成题组失败时不创建未完成练习；报告失败时保留 `ready_for_report`，不能删除用户的答题数据。

### 5.2 当前题目位置

1. `current_index` 表示用户下次进入时要看到的题目索引。
2. 用户只选择选项但没有点击“提交答案”时，不要求保存未提交选择；重新进入后可以重新选择。
3. 用户点击“提交答案”后，必须立即保存该题的答题记录。
4. 用户查看解析后还未点击下一题就离开，重新进入时仍停留在该题，并允许继续到下一题。
5. 重新进入时不得跳到已提交题目的前面，也不得跳过未提交题目。

### 5.3 返回行为

答题页左上角返回按钮的正确流程：

1. 保存当前会话和答题进度。
2. 保留练习状态为 `in_progress` 或 `ready_for_report`。
3. 返回答题首页。
4. 首页刷新未完成练习模块。
5. 不清空当前练习数据，不生成报告，不增加完成经验值。

### 5.4 完成行为

1. 用户完成最后一道题后，状态变为 `ready_for_report`。
2. 报告生成成功后，状态更新为 `completed`。
3. 只有状态更新成功后，首页才移除该练习。
4. 报告生成失败时，继续展示该练习，并提供重新生成报告的入口。
5. 完成经验值只能结算一次，重复打开报告或重复请求不能重复增加 XP。

### 5.5 多设备与登录规则

1. 已登录用户的未完成练习以用户账号为归属，换设备登录后可以读取。
2. 未登录用户可以在当前设备暂存未完成练习，但不能保证跨设备恢复。
3. 游客登录后，应将本地未完成练习与账号下同 `quiz_id` 的记录合并，避免重复创建。
4. 同一用户的同一 `quiz_id` 只能存在一条进行中记录。
5. 账号退出登录后，页面不得继续展示上一个用户的未完成练习。

## 6. 首页未完成练习模块需求

### 6.1 展示位置

未完成练习模块放在首页用户问候区域之后、输入新主题区域之前，保证用户打开答题首页即可看到。

### 6.2 空状态

当用户没有未完成练习时：

1. 不展示空白大卡片。
2. 可以只保留“开始新的练习”主流程。
3. 不使用虚假的示例练习冒充真实记录。

### 6.3 单条练习信息

每条未完成练习至少展示：

| 字段 | 展示内容 |
| --- | --- |
| 主题 | 用户输入的技术主题或题组标题 |
| 岗位方向 | 如 AI / 大模型、后端开发 |
| 难度 | 基础、中等、进阶 |
| 进度 | 已完成题数 / 总题数 |
| 状态 | 进行中或待生成报告 |
| 时间 | 最近保存时间，如“今天 19:30” |
| 操作 | “继续答题”或“生成报告” |

### 6.4 交互规则

1. 点击整条卡片或“继续答题”进入答题页。
2. `in_progress` 进入上次题目位置。
3. `ready_for_report` 进入报告生成页，不要求用户重复答题。
4. 支持删除时，删除只影响该用户的未完成记录，不影响已完成历史。
5. 加载失败时展示可重试状态，不影响用户继续创建新练习。

## 7. 数据需求

### 7.1 练习进度必须保存的数据

每组未完成练习至少需要保存：

| 字段 | 说明 |
| --- | --- |
| `quiz_id` | 题组唯一标识 |
| `user_id` | 登录用户标识；游客记录可为空 |
| `topic` | 用户输入主题 |
| `role` | 岗位方向 |
| `difficulty` | 练习难度 |
| `questions` | 完整题目内容，保证离线/恢复时题目一致 |
| `answer_records` | 已提交的答题记录 |
| `current_index` | 下次恢复的题目索引 |
| `status` | 练习状态 |
| `created_at` | 创建时间 |
| `updated_at` | 最近进度保存时间 |
| `completed_at` | 完成时间，可为空 |
| `report` | 报告内容，可为空 |

### 7.2 数据一致性要求

1. 保存进度必须幂等，同一 `quiz_id` 重复保存不能产生重复记录。
2. `answer_records` 中不能出现不存在的题目 ID。
3. `current_index` 必须位于 `0` 到题目总数之间。
4. 已完成练习不能被普通的进度保存请求回退为进行中。
5. 报告保存与完成状态更新必须保持一致，避免出现“首页消失但报告不存在”。

## 8. 关键异常与边界情况

| 情况 | 期望行为 |
| --- | --- |
| 返回按钮保存失败 | 提示用户保存失败，保留当前页面，不静默丢失进度 |
| 网络断开但已有本地缓存 | 先保存本地，恢复网络后重试服务端同步 |
| 同一账号多设备同时答题 | 以最后一次成功保存的 `updated_at` 为准，并避免覆盖更新版本的数据 |
| 题组数据损坏 | 不进入空白页，提示练习不可恢复，并允许返回首页 |
| 用户重复点击继续答题 | 防止重复跳转和重复请求 |
| 报告接口重复提交 | 后端幂等返回已有报告，不重复加 XP |
| 登录失效 | 清理当前用户状态，提示重新登录，不展示其他用户数据 |
| 用户主动删除 | 二次确认后将状态改为 `abandoned`，默认不可恢复 |

## 9. 验收标准

### 9.1 返回与恢复

- [ ] 答题第 1 题提交后点击左上角返回，首页显示该练习进度。
- [ ] 重新点击该练习，进入上次离开的题目，而不是重新开始。
- [ ] 已提交答案、正确性和题目顺序保持不变。
- [ ] 切换到“我的”、关闭小程序后重新打开，进度仍可恢复。

### 9.2 首页模块

- [ ] 登录用户每次进入首页都能加载自己的未完成练习。
- [ ] 没有未完成练习时不显示虚假数据。
- [ ] 多组练习可以同时展示并按最近更新时间排序。
- [ ] `ready_for_report` 练习显示“生成报告”，而不是“继续答题”。

### 9.3 完成与历史

- [ ] 报告成功生成后，未完成模块移除该练习。
- [ ] 该练习进入历史记录并可以查看报告详情。
- [ ] 报告生成失败时，练习仍保留且可以重试。
- [ ] 同一练习重复生成报告不会重复增加 XP。

### 9.4 用户隔离

- [ ] 用户 A 看不到用户 B 的未完成练习。
- [ ] 退出登录后不残留上一个用户的练习数据。
- [ ] 用户重新登录后能恢复自己的未完成练习。

## 10. 实施优先级建议

建议按以下顺序实现：

1. 先改造本地会话模型和返回按钮，确保当前设备可以断点续答。
2. 增加后端练习进度保存、查询和恢复接口。
3. 首页增加未完成练习模块，并接入登录用户数据。
4. 补充报告失败状态和幂等完成逻辑。
5. 最后实现多组练习、删除练习和本地进度合并。

本需求完成后，EasyOffer 的用户系统将从“只保存已完成历史”升级为“同时管理进行中的学习任务和已完成的复盘记录”。
